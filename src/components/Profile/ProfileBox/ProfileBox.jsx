import styled from "styled-components";
import { color, space, typography, layout, border, shadow } from "styled-system";

export const ProfileBox = styled('div')(color, space, typography, layout, border, shadow )